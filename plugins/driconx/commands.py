import dric
import os.path
import inspect

class FromBinding(object):
    def __init__(self, binding):
        module = inspect.getmodule(binding)
        self.list_of_commands
        module.enums['MAV_CMD']
    def getResponse(self):
        dric.Response('ok')
